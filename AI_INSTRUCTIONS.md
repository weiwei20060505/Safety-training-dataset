# AI 專案協助注意事項 (AI Instructions)

這份文件記錄了本專案（LLM 隱藏狀態機率校正與元評估框架）的核心業務邏輯、技術坑點與 AI 協作時必須遵守的注意事項，以便 AI 在未來的對話中能快速進入狀況，避免給出錯誤的建議。

## 1. 核心任務轉換邏輯 ($y_2 \to y_3$) 🚨 絕對不可忘記
*   **定義**：
    *   $y_1$：LLM 回覆安全性 (Safe=0, Unsafe=1)
    *   $y_2$：輸入 Prompt 有害性 (Benign=0, Harmful=1)
    *   $y_3$：安全判定一致性 ($y_3 = \mathbb{I}(y_1 == y_2)$)
*   **策略**：模型先訓練去預測 **$y_2$**（因為最準），接著透過以下邏輯翻轉為 $y_3$ 的先驗分數，最後再送入 PAVA (Isotonic Regression) 進行校正：
    ```python
    # p 為模型預測 y2=1 的機率
    pre_cal_score = np.where(y1 == 1, p, 1 - p)
    ```
*   **AI 警告**：當使用者詢問校正邏輯時，**必須**將這個 `np.where` 的轉換考慮進去，不可直接拿 $y_2$ 的機率去做後續推論。

## 2. V1 痛點與 V2 核心解法 (條件分流)
*   **V1 的致命傷 (條件校正漂移)**：V1 將上述翻轉過後的 $y_1=0$ 分數 ($1-p$) 與 $y_1=1$ 分數 ($p$) **混在同一個池子裡做全域 Isotonic 校正**。這無視了兩群資料的條件異質性，導致嚴重的 Subgroup Calibration Drift。
*   **V2 的解法**：從底層網路（如 YHead-MLP、Hard-Dual）到後端校正，全部依據 $y_1=0$ 與 $y_1=1$ **強制拆開、獨立執行 PAVA 校正**。

## 3. Git 同步與檔案遺失問題 (Gitignore 陷阱)
專案的 `.gitignore` 設定非常嚴格：
*   **忽略同步**：`models/` (模型權重)、`cache/`、以及全域的 `*.pkl` 和 `*.csv`。
*   **`results/` 白名單**：`results/` 底下**只允許**同步 `.png`, `.txt`, `.log`, `.md`。
*   **AI 警告**：如果使用者抱怨「換了一台電腦後找不到最新的模型或 CSV 數據」，請立刻提醒使用者這是 `.gitignore` 的阻擋導致的，必須手動透過隨身碟或雲端硬碟同步這些二進制檔與數據檔。

## 4. 歷史數據與模型範圍 (RF 被排除)
*   在 `pipeline_v1_baseline/unified_train.py` 的實作中，`models_to_train` 陣列手動排除了 Random Forest (`RF`)。
*   **AI 警告**：這導致目前產出的 `cache/` 快取、`models/` 權重以及所有圖表，**都只有 MLP, LGB, LR 這三個模型**。在幫使用者畫圖或寫 Markdown 總結時，不可憑空捏造 RF 的數據。

## 5. 未來研究方向 (Future Work)
*   **細粒度分群與可交換性**：專案下一步將不僅依賴 $y_1$ 分流，還要加入資料集內建的 **「對抗樣本 (Adversarial) vs 常規樣本 (Vanilla)」** 提示詞分類，進行更細粒度的 Grouping。
*   **目標**：透過分群抽樣或校正，讓 Train, Test, Eval 資料集之間具備統計上的 **可交換性 (Exchangeability)**，以抵抗 Domain Shift。

## 6. 畫圖與分析腳本規範
*   **出圖路徑**：所有利用腳本產出的新圖表，請務必按照 V1/V2 的架構，存放到 `results/v1_baseline/plots_custom/` 或 `results/v2_framework/...` 對應的資料夾下。
*   **中文字型**：作圖時遇到中文，務必在腳本中加入 `plt.rcParams['font.sans-serif'] = ['Microsoft JhengHei', 'SimHei', ...]` 以避免豆腐塊亂碼。

## 7. 🚫 系統安全與資源限制 (嚴禁主動讀取大檔案)
*   **絕對禁令**：除非使用者明確下達指令，否則 **嚴禁 AI 主動使用腳本或讀檔工具讀取 `data/` 資料夾下的任何原始數據檔案**（包含巨大的 PKL、JSON 或 CSV 等）。
*   **嚴重後果**：這些資料集檔案體積極度龐大，若 AI 在背景自動讀取解析，會瞬間撐爆記憶體（OOM），導致使用者的電腦直接死機 / 當機！若需分析數據，請僅讀取 `cache/` 下輕量化的指標檔，或要求使用者提供小批次抽樣樣本。

## 8. 📝 行動前計畫與確認 (Plan Before Action)
*   **規劃先行**：當需要修改程式碼、更動專案架構或執行會改變系統狀態的任務前，AI **必須先擬定詳細的執行計畫 (Plan)**。
*   **等待授權**：提出計畫後，必須暫停並等待使用者同意 (Approval) 後才能開始行動。
*   **例外情況**：純粹的資料讀取、數據查詢、或是「沒有做任何更改」的解答與分析，可以不用事前審批，直接提供結果。
*   **紀錄執行結果**：每次執行完計畫後，必須將執行過程詳細整合並「附加 (Append)」至專案根目錄的 `執行結果.md` 檔案中。必須詳細紀錄：計畫內容、修改了什麼、執行了哪些程式碼、生成了什麼檔案以及細節。**絕對不可刪除舊有的紀錄**，只能加在檔案最後面。

## 9. 🌐 語言與溝通規範
*   **繁體中文**：AI 與使用者的對話、撰寫的 Markdown 報告以及註解，**必須一律使用繁體中文 (Traditional Chinese)** 進行回覆。

---

## 🎯 短期計畫 (Short-term Plan)
*註：AI 在執行完下列任務後，可將其從清單中刪除。*
1. **調整 V1 LGB 參數**：修改 V1 版的 LGB 參數（採額外新增的方式，不覆蓋原本的配置），實驗看看是否是因為原本參數設定問題，導致 LGB 在 V1 效果不佳。
2. **全盤驗證與重跑**：檢查所有程式碼邏輯，確保與預期理論無誤。確認無誤後，刪除舊有的快取與模型檔（`cache/`, `models/`, `results/` 等），然後全部重新執行，確保圖表和資料都是最新鮮的。
3. **擴增資料集**：嘗試增加目前 84,999 筆的數據量。
