# 🗺️ LLM 安全防護特徵探針與信賴度機率校正系統的完備化

### 📊 專案整體進度摘要

- **目前核心目標：** 基於大型語言模型（LLM）內部的隱藏狀態特徵（Hidden States），訓練高靈敏度且輕量化的安全探針（Probes），全面預測 **y1 模型回覆安全性**、**y2 提示詞有害性** 與 **y3 安全判定一致性**。同時導入 **保序迴歸（Isotonic Regression）** 機率校正，消除下採樣導致的機率扭曲，確保分類器的預測分數具備真實的信賴度。
- **當前實作狀態：** 🟢 **已完成全專案 PCA 移除、統一機率校正與繪圖渲染優化**
  1. **完全移除 PCA 降維**：模型訓練與校正全數直接使用 1,024 維原始 Hidden State 特徵，並消除所有 CLI `--use_pca` / `--no_pca` 開關。
  2. **簡化資料夾架構**：全面淘汰 `with_pca` 與 `without_pca` 二分法子資料夾，快取與圖表路徑統一標準化。
  3. **統一 Isotonic 機率校正**：修復先前因 $y_1$ 分拆校正導致的非單調鋸齒問題，改採統一 Isotonic Regression 擬合 $y_3 = \mathbb{I}(y_1 == y_2)$。
  4. **階梯曲線轉折點遮罩**：實作 `np.diff(y_step) != 0` 轉折點過濾，完美解決重疊平移點導致 Matplotlib 產生灰色鋸齒殘影的視覺瑕疵。
  5. **批次執行檔整合**：統一整合為 `run_full_v1.bat` 與 `run_full_v2.bat` 兩大一鍵腳本。

---

### 🧱 系統架構與邏輯

> [!info] 核心運作邏輯
> 1. **基礎探針訓練 (`unified_train.py` & `train_framework_models.py`)**：在基準訓練集中，針對隱藏層提取 1024 維特徵，經 `StandardScaler` 後直接訓練分類器模型。
> 2. **獨立測試集與評估機制**：載入 3 個獨立測試集 `test1` (10,000筆)、`test2` (10,000筆) 與 `eval` (2,210筆) 用於模型校準與評估。
> 3. **統一機率校正 (`step2_calibrate.py` & `calibrate_framework_models.py`)**：使用 `test1` 作為 PAVA 校準器的 Fit Set，輸出校準後機率與快取。
> 4. **核心視覺化 (`plot_v1_custom.py` & `plot_framework_stage2_v2.py`)**：產出高清指標趨勢圖、可靠度曲線圖與Confidence 雙 Y 軸聯合校正圖 (Joint Calibration)。

---

### 🛠️ 方案評估與最終決策

| 曾嘗試過的舊邏輯 | 🛑 遇到的問題與痛點 | 🟢 最終採用的優化方案與優勢 |
| :--- | :--- | :--- |
| **PCA(128) 降維與 with_pca/without_pca 資料夾** | 降維損失神經元資訊，且二分法目錄增加維護複雜度與參數判斷。 | **完全移除 PCA 與簡化目錄**<br>全專案統一基於 1024 維原始特徵處理，刪除所有 PCA 旗標與二分法資料夾。 |
| **$y_1$ 分拆校正 (iso_0 / iso_1)** | 混淆相同預測分數下的校正機率，破壞 Isotonic Regression 絕對單調遞增特性，繪圖呈現上下鋸齒。 | **統一 Isotonic 校正**<br>改為對全量資料進行統一 Isotonic 擬合，確保校正曲線嚴格單調遞增。 |
| **Matplotlib 密集點繪圖視覺瑕疵** | 數千個 Y 值相同的密集平移點在畫 `steps-post` 時重疊，形成灰色粗糙色塊。 | **轉折點遮罩 (Turning Point Mask)**<br>加入 `np.diff(y_step) != 0` 遮罩，僅繪製發生跳躍的轉折點，畫質乾淨平滑。 |
| **多個分散的 v1 .bat 批次檔** | 存在 `run_v1_without_pca.bat`, `run_v1_lgb_expansion.bat` 等多個批次檔，易生混淆。 | **整合一鍵批次檔**<br>統一整合成 `run_full_v1.bat` 與 `run_full_v2.bat`。 |
