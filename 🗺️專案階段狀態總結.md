# 🗺️ LLM 安全防護特徵探針與信賴度機率校正系統的完備化

### 📊 專案整體進度摘要

- **目前核心目標：** 基於大型語言模型（LLM）內部的隱藏狀態特徵（Hidden States），訓練高靈敏度且輕量化的安全探針（Probes），全面預測 **y1 模型回覆安全性**、**y2 提示詞有害性** 與 **y3 安全判定一致性**。同時導入 **雙軌保序迴歸（Isotonic Regression）** 機率校正，消除下採樣導致的機率扭曲，確保分類器的預測分數具備真實的信賴度。
- **當前實作狀態：** 🟢 **已完成新超參數調參、重新訓練、校正與局部出圖** _(說明：已對齊 V1 與 V2 的最新調參配置。LGBM 模型使用 max_depth=4，RootSplit_LGBM 使用 max_depth=5 進行深度補償；L1 及 L2 正則化均調高至 0.5；且 min_data_in_leaf 設為 20。MLP 架構中，optimizer 的 weight_decay 調高至 1e-2，且 dropout_rate 設為 0.5。目前已完成對應 V2 模型的重新訓練與保序校準，並僅繪製出 test2 評估集下 RootSplit_LGBM 的 2x4 聯合校正雙 Y 軸合併圖！)_

---

### 🧱 最終定案之系統架構與邏輯

> [!info] 核心運作邏輯
> 目前最新、定案的系統資料流與架構主要分為以下三個步驟：
> 
> 1. **基礎探針訓練 (`unified_train.py` & `train_framework_models.py`)**：在 10,000 筆基準訓練集中，針對隱藏層提取特徵，經 `StandardScaler` 後，支援是否經過 `PCA(128)` 降維，訓練各個分類器模型，並動態自適應特徵輸入維度。
> 2. **獨立測試集與評估機制**：載入 3 個獨立測試集 `test1` (10,000筆)、`test2` (10,000筆) 與 `eval` (2,210筆) 用於模型校準與評估。
> 3. **雙軌條件機率校正 (`calibrate_framework_models.py`)**：使用 `test1` 作為 PAVA 校準器的 Fit Set，依據 $y_1 == 0$ 與 $y_1 == 1$ 條件分流擬合，並在各測試集上輸出校準後機率。
> 4. **階段二核心視覺化 (`plot_framework_stage2_v2.py`)**：依據有無 PCA 模式以及評估集類型，分流產出高清小圖與各類合併大圖。

---

### 🗂️ 修正後之圖表目錄結構 (`results/v2_framework/plots_framework_stage2/`)

所有 combined 圖檔在輸出時，均會依據 **PCA 模式** (`with_pca` / `without_pca`) 與 **評估集** (`test1` / `test2` / `eval`) 進行層級分類存放：

```text
results/v2_framework/plots_framework_stage2/
├── 01_Metrics_Trends_split_y/
│   ├── single/
│   │   ├── with_pca/
│   │   │   ├── test1/          # 存放隨層數變化指標趨勢單圖 (有 PCA, test1)
│   │   │   └── ... (test2, eval)
│   │   └── without_pca/
│   └── combined/
│       ├── with_pca/
│       │   ├── test1/          # 存放 1x2 趨勢合併圖 (有 PCA, test1)
│       │   ├── test2/          # 存放 1x2 趨勢合併圖 (有 PCA, test2)
│       │   ├── eval/           # 存放 1x2 趨勢合併圖 (有 PCA, eval)
│       │   └── combined_brier_trends_split_y_3x2.png  # 全評估集對比 3x2 大圖
│       └── without_pca/
├── 02_Reliability_Curves_combined/
│   ├── single/
│   │   ├── with_pca/
│   │   └── without_pca/
│   └── combined/
│       ├── with_pca/
│       │   ├── test1/          # 存放 1x4 可靠度合併圖 (有 PCA, test1)
│       │   ├── test2/          # 存放 1x4 可靠度合併圖 (有 PCA, test2)
│       │   ├── eval/           # 存放 1x4 可靠度合併圖 (有 PCA, eval)
│       │   └── y3_RootSplit_LGBM_combined_reliability_3x4.png # 3x4 全評估集對比大圖
│       └── without_pca/
└── 07_Joint_Calibration/
    ├── single/
    │   ├── with_pca/
    │   └── without_pca/
    └── combined/
        ├── with_pca/
        │   ├── test1/          # 存放 2x4 聯合校正雙 Y 軸合併圖 (有 PCA, test1)
        │   ├── test2/          # 存放 2x4 聯合校正雙 Y 軸合併圖 (有 PCA, test2)
        │   ├── eval/           # 存放 2x4 聯合校正雙 Y 軸合併圖 (有 PCA, eval)
        │   └── (各模型獨立之 2x4 合併圖)
        └── without_pca/
```

---

### 🛠️ 方案評估與最終決策

| 曾嘗試過的舊邏輯 | 🛑 遇到的問題與痛點 | 🟢 最終採用的優化方案與優勢 |
| :--- | :--- | :--- |
| **硬編碼 128 維與 129 維命名** | 當不使用 PCA 降維時，輸入維度會發生改變，原 129 或 128 的類別命名與變數命名會引起混淆。 | **去維度硬編碼與自適應維度**<br>重命名為 `FeaturePlusY1LGBMClassifier`, `SingleHeadMLPNet` 等，且 `input_dim` 改由實例化時動態傳入。 |
| **LGBM 深度與正則化不足** | 原本使用預設深（5 或 10）與較弱的正則化，容易在 LLM 隱藏特徵上發生輕微過擬合。 | **樹深限制與強正則化（最新調參）**<br>限制 `max_depth=4`（RootSplit 使用 5 作為深度補償）；同時將 L1 及 L2 正則化（`reg_alpha`/`reg_lambda`）大幅提高至 `0.5`，並限制 `min_data_in_leaf=20` 以提升泛化力。 |
| **MLP 防過擬合機制較弱** | V2 原有的 MLP 包含深層多層結構，dropout_rate=0.2 且 weight_decay=1e-4 可能在無 PCA 的高維輸入下發生過擬合。 | **提高 MLP 正則化與 Dropout（最新調參）**<br>將 MLP 的 `dropout_rate` 提高至 `0.5`，且 Optimizer 中的 `weight_decay` 提高至 `1e-2`，以加強在高維特徵上的泛化效果。 |
| **combined 下無 PCA 模式與評估集分流** | with_pca 與 without_pca 的圖表會發生覆蓋，且 test1, test2, eval 的合併圖與小圖混雜在同一目錄中。 | **combined 目錄二級重構**<br>在 combined 目錄下依據 `{pca_status}/{split}/` 做明確歸類。除了生成跨評估集的全域拼圖（3x2、3x4）外，也額外針對個別評估集自動產出專屬的 1x2 與 1x4 合併圖，分類結構完美吻合實驗對比需求。 |